#include <iostream>
#include <stack>
#include <string>
#include <sstream>
#include <cmath> // for pow

using namespace std;

double evaluatePostfix(const string& postfix) {
    stack<double>operandstack;
    string (postfix);
    string operation;
    while(rnff)
